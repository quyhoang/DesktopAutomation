Cv = 1.78;
h = 15; % stroke in mm
B_deg = 40;
B = deg2rad(B_deg);

list_rRoller = linspace(1,100,1000); % roller radius in mm
primeRadius = Cv*h/B*sqrt(3)-h/2;
list_rBase = primeRadius - list_rRoller;



maxTan1 = Cv*h/B;
maxTan2 = h/2 + rBase + rRoller;
maxTan = maxTan1/maxTan2;
maxAlpha = rad2deg(atan(maxTan))